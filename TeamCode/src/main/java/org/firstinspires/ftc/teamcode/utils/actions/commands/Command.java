package org.firstinspires.ftc.teamcode.utils.actions.commands;

/**
 * An abstract class to represent commands to be executed on the robot, such as moving a servo.
 */
public interface Command {
    /**
     * Runs the command.
     */
    void run();
}
